Los dos SQLs están aplicados en Supabase:
- notification_create con fixed_slot_assigned 
  y fixed_slot_released ✅
- get_player_calendar con turnos fijos ✅

Cerrar el feature/turnos-fijos:

1. npm run build — verificar sin errores

2. git add -A
   git commit -m "feat: turnos fijos — asignación 
   recurrente semanal por club, grid violeta, 
   tab gestión, calendario jugador con ↻"

3. git checkout main
   git merge --no-ff feature/turnos-fijos \
     -m "feat: turnos fijos para clubes"

4. git tag v0.10.3
   git push origin main
   git push origin --tags

5. git branch -d feature/turnos-fijos
   git push origin --delete feature/turnos-fijos

6. Documentar las migraciones SQL aplicadas 
   manualmente en supabase/migrations/:
   - 20260416_010_court_fixed_slots.sql
   - 20260416_011_club_get_agenda_slots_fixed.sql
   - 20260416_012_notification_create_fixed_slots.sql
   - 20260416_013_get_player_calendar_fixed_slots.sql
   Marcar cada uno con:
   -- Already applied in Supabase on 2026-04-16
   -- Documentation only

7. Actualizar CLAUDE.md con v0.10.3