El panel /player/mi-club funciona correctamente.

1. Quitar todos los console.log de diagnóstico
   que agregamos en requireClubOwner():
   - [requireClubOwner] player.id
   - [requireClubOwner] player.is_club_owner
   - [requireClubOwner] club encontrado
   - [requireClubOwner] error

2. npm run build

3. git add -A
   git commit -m "fix: limpiar logs diagnóstico 
   requireClubOwner"
   git push origin main

4. git tag v0.10.1
   git push origin --tags